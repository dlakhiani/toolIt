/**
 * barrel file: consolidate and re-export
 * modules from *interfaces* => easier to import elsewhere
 */
export { TUser } from "./user.interface.ts"
export { TVehicle } from "./vehicle.interface.ts"
